{
  "name": "ceo-office",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix frontend-new run dev",
    "build": "npm --prefix frontend-new run build",
    "preview": "npm --prefix frontend-new run preview",
    "install:frontend": "npm --prefix frontend-new install",
    "start:backend": "cd backend && uvicorn server:app --host 0.0.0.0 --port 8001 --reload"
  }
}
